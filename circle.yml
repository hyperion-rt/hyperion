machine:
  services:
    - docker

dependencies:
  override:
    - docker pull astrofrog/hyperion-ci:1.0

checkout:
  post:
    - git submodule init
    - git submodule update

test:
  override:
    - ./.run_docker_tests.sh
